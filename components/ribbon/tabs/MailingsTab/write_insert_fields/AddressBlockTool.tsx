
import React from 'react';
import { BookUser } from 'lucide-react';
import { RibbonButton } from '../../../common/RibbonButton';

export const AddressBlockTool: React.FC = () => (
    <RibbonButton icon={BookUser} label="Address Block" onClick={() => {}} />
);
