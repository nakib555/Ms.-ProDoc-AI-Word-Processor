
import React from 'react';
import { Code } from 'lucide-react';
import { RibbonButton } from '../../../common/RibbonButton';

export const InsertMergeFieldTool: React.FC = () => (
    <RibbonButton icon={Code} label="Insert Merge Field" onClick={() => {}} hasArrow />
);
