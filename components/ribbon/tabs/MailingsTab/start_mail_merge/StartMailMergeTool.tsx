
import React from 'react';
import { MailPlus } from 'lucide-react';
import { RibbonButton } from '../../../common/RibbonButton';

export const StartMailMergeTool: React.FC = () => (
    <RibbonButton icon={MailPlus} label="Start Mail Merge" onClick={() => {}} hasArrow />
);
