
import React from 'react';
import { Users } from 'lucide-react';
import { RibbonButton } from '../../../common/RibbonButton';

export const SelectRecipientsTool: React.FC = () => (
    <RibbonButton icon={Users} label="Select Recipients" onClick={() => {}} hasArrow />
);
