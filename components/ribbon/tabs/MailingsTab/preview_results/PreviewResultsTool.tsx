
import React from 'react';
import { ScanEye } from 'lucide-react';
import { RibbonButton } from '../../../common/RibbonButton';

export const PreviewResultsTool: React.FC = () => (
    <RibbonButton icon={ScanEye} label="Preview Results" onClick={() => {}} />
);
