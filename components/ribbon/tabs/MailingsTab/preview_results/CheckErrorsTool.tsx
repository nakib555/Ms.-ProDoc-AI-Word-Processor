
import React from 'react';
import { ShieldCheck } from 'lucide-react';
import { SmallRibbonButton } from '../common/MailingsTools';

export const CheckErrorsTool: React.FC = () => (
    <SmallRibbonButton icon={ShieldCheck} label="Check for Errors" onClick={() => {}} />
);
