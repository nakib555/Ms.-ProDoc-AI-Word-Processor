
import React from 'react';
import { ArrowRight } from 'lucide-react';
import { SmallRibbonButton } from '../common/ReviewTools';

export const NextChangeTool: React.FC = () => (
    <SmallRibbonButton icon={ArrowRight} label="Next" onClick={() => {}} />
);
