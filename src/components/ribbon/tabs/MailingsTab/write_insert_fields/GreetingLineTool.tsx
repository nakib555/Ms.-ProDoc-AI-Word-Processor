
import React from 'react';
import { Hand } from 'lucide-react';
import { RibbonButton } from '../../../common/RibbonButton';

export const GreetingLineTool: React.FC = () => (
    <RibbonButton icon={Hand} label="Greeting Line" onClick={() => {}} />
);
