
import React from 'react';
import { Highlighter } from 'lucide-react';
import { RibbonButton } from '../../../common/RibbonButton';

export const HighlightMergeFieldsTool: React.FC = () => (
    <RibbonButton icon={Highlighter} label="Highlight Merge Fields" onClick={() => {}} />
);
