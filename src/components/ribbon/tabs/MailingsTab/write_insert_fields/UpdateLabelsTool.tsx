
import React from 'react';
import { RefreshCw } from 'lucide-react';
import { SmallRibbonButton } from '../common/MailingsTools';

export const UpdateLabelsTool: React.FC = () => (
    <SmallRibbonButton icon={RefreshCw} label="Update Labels" onClick={() => {}} />
);
