
import React from 'react';
import { Workflow } from 'lucide-react';
import { SmallRibbonButton } from '../common/MailingsTools';

export const RulesTool: React.FC = () => (
    <SmallRibbonButton icon={Workflow} label="Rules" onClick={() => {}} hasArrow />
);
