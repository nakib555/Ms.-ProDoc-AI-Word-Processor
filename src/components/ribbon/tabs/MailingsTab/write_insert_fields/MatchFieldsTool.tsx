
import React from 'react';
import { ArrowRightLeft } from 'lucide-react';
import { SmallRibbonButton } from '../common/MailingsTools';

export const MatchFieldsTool: React.FC = () => (
    <SmallRibbonButton icon={ArrowRightLeft} label="Match Fields" onClick={() => {}} />
);
