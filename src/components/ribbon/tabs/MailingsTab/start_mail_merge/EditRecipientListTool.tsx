
import React from 'react';
import { UserCog } from 'lucide-react';
import { RibbonButton } from '../../../common/RibbonButton';

export const EditRecipientListTool: React.FC = () => (
    <RibbonButton icon={UserCog} label="Edit Recipient List" onClick={() => {}} />
);
