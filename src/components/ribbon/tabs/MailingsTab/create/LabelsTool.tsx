
import React from 'react';
import { Sticker } from 'lucide-react';
import { RibbonButton } from '../../../common/RibbonButton';

export const LabelsTool: React.FC = () => (
    <RibbonButton icon={Sticker} label="Labels" onClick={() => {}} />
);
