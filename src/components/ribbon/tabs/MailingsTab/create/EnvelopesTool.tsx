
import React from 'react';
import { Mail } from 'lucide-react';
import { RibbonButton } from '../../../common/RibbonButton';

export const EnvelopesTool: React.FC = () => (
    <RibbonButton icon={Mail} label="Envelopes" onClick={() => {}} />
);
