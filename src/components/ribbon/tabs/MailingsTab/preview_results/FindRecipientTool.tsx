
import React from 'react';
import { Search } from 'lucide-react';
import { SmallRibbonButton } from '../common/MailingsTools';

export const FindRecipientTool: React.FC = () => (
    <SmallRibbonButton icon={Search} label="Find Recipient" onClick={() => {}} />
);
