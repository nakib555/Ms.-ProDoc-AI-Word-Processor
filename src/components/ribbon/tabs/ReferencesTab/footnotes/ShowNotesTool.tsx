
import React from 'react';
import { Eye } from 'lucide-react';
import { SmallRibbonButton } from '../common/ReferencesTools';

export const ShowNotesTool: React.FC = () => (
    <SmallRibbonButton icon={Eye} label="Show Notes" onClick={() => {}} />
);
