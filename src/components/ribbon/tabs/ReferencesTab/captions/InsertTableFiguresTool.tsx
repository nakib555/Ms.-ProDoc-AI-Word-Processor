
import React from 'react';
import { Table } from 'lucide-react';
import { SmallRibbonButton } from '../common/ReferencesTools';

export const InsertTableFiguresTool: React.FC = () => (
    <SmallRibbonButton icon={Table} label="Insert Table of Figures" onClick={() => {}} />
);
