
import React from 'react';
import { Scale } from 'lucide-react';
import { SmallRibbonButton } from '../common/ReferencesTools';

export const InsertTableAuthoritiesTool: React.FC = () => (
    <SmallRibbonButton icon={Scale} label="Insert Table of Authorities" onClick={() => {}} />
);
