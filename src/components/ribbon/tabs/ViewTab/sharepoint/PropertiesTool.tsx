
import React from 'react';
import { List as ListIcon } from 'lucide-react';
import { RibbonButton } from '../../../common/RibbonButton';

export const PropertiesTool: React.FC = () => (
    <RibbonButton icon={ListIcon} label="Properties" onClick={() => {}} />
);
