
import React from 'react';
import { Type } from 'lucide-react';
import { SmallRibbonButton } from '../../../../../ViewTab/common/ViewTools';

export const abcTextTool: React.FC = () => (
    <SmallRibbonButton icon={Type} label="abc Text" onClick={() => {}} />
);
