
import React from 'react';
import { FileEdit } from 'lucide-react';
import { SmallRibbonButton } from '../common/ReviewTools';

export const ReviewingPaneTool: React.FC = () => (
    <SmallRibbonButton icon={FileEdit} label="Reviewing Pane" onClick={() => {}} hasArrow />
);
